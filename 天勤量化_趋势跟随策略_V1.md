# 期货趋势跟随交易策略 - 天勤量化版本

> 版本：V1.0
> 作者：咕噜 AI
> 日期：2026-03-19

---

## 一、策略概述

### 1.1 策略名称
**天勤趋势跟随策略（TQTrendFollowing）**

### 1.2 策略定位
- 适用于期货日内交易和隔夜趋势交易
- 主要交易品种：螺纹钢(RB)、热卷(HC)、焦炭(J)、焦煤(JM)、甲醇(MA)、玻璃(FG)、纯碱(SA)、豆粕(M)、菜粕(RM)、白糖(SR)
- 周期：5分钟、15分钟、日线

### 1.3 核心理念
> 基于走势结构的趋势跟随
> - 顺势而为，不逆势交易
> - 截断亏损，让利润奔跑
> - 机械执行，避免主观判断

---

## 二、入场条件（满足全部）

### 2.1 趋势判断
```
条件1：MA20 方向向上（做多）/向下（做空）
条件2：价格站在MA5均线之上（做多）/之下（做空）
```

### 2.2 形态信号
```
条件3：最近3根K线出现 顶分型（做空）/底分型（做多）
条件4：价格突破最近3根K线的最高点（做多）/最低点（做空）
```

### 2.3 力度验证（可选加分项）
```
条件5：创新高/新低时成交量放大
条件6：MACD黄白线在零轴上方（金叉）/下方（死叉）
```

---

## 三、仓位管理

### 3.1 仓位计算
```python
# 每次开仓手数计算
单笔风险金额 = 账户权益 × 风险比例(1-2%)
止损价差 = 入场价 - 止损价
开仓手数 = 单笔风险金额 / (止损价差 × 合约乘数 × 保证金比例)
```

### 3.2 风险参数
| 参数 | 默认值 | 说明 |
|------|--------|------|
| 风险比例 | 1% | 单笔最大亏损 |
| 最大持仓 | 3个合约 | 同一时间最多持有 |
| 最大仓位 | 30% | 总仓位不超过 |

### 3.3 品种选择优先级
1. 成交量 > 100万手/日
2. 波动幅度适中（螺纹钢最佳）
3. 趋势明显时优先做龙一品种

---

## 四、止损止盈

### 4.1 止损规则
```
固定止损：入场后亏损 1.5% 平仓
移动止损：盈利达到 2% 后，启动保本止损（0.5%）
```

### 4.2 止盈规则
```
目标1：盈利 3% 止盈1/3
目标2：盈利 5% 止盈1/3  
目标3：盈利 8% 止盈1/3
或者：有效跌破MA20 全部平仓
```

### 4.3 特殊情况
- 收盘前 5分钟无论盈亏全部平仓（日内策略）
- 周末持仓不超过 20%

---

## 五、风控原则

### 5.1 交易纪律（必须遵守）
1. 严格按信号执行，不主观交易
2. 每日最大亏损 3% 停止交易
3. 每周最大亏损 8% 停止交易
4. 每月最大亏损 15% 停止交易

### 5.2 禁止行为
- ❌ 逆势抄底摸顶
- ❌ 扛单（不止损）
- ❌ 重仓（>30%）
- ❌ 频繁交易（每天>5笔）
- ❌ 亏损后报复性交易

---

## 六、天勤量化代码框架

```python
"""
天勤量化 - 趋势跟随策略
支持天勤 TQSDK
"""
from tqsdk import TqApi, TqAuth
from tqsdk.ta import MA, MACD
from tqsdk.lib import TargetPosTask
import time

class TrendFollowingStrategy:
    def __init__(self, config):
        # 基础配置
        self.api = TqApi(auth=TqAuth(config['account'], config['password']))
        self.symbol = config['symbol']  # 合约代码，如 'SHFE.rb2505'
        self.account = config['account_equity']  # 账户权益
        
        # 策略参数
        self.risk_ratio = config.get('risk_ratio', 0.01)  # 风险比例 1%
        self.ma_period = config.get('ma_period', 20)     # MA周期
        self.stop_loss_pct = config.get('stop_loss_pct', 0.015)  # 止损比例
        self.profit_target = config.get('profit_target', [0.03, 0.05, 0.08])  # 止盈目标
        
        # 持仓状态
        self.position = 0  # 持仓手数
        self.entry_price = 0  # 开仓价格
        self.trade_day = None  # 交易日期
        
    def get_ma(self, n=20):
        """获取均线数据"""
        klines = self.api.get_kline_serial(self.symbol, n * 60 * 1000, n + 10)
        ma = MA(klines, n)
        return ma.iloc[-1]['MA']
    
    def check_trend(self):
        """检查趋势方向"""
        klines = self.api.get_kline_serial(self.symbol, 15 * 60 * 1000, 100)
        ma5 = MA(klines, 5)
        ma20 = MA(klines, 20)
        
        current_price = klines.iloc[-1]['close']
        ma5_val = ma5.iloc[-1]['MA']
        ma20_val = ma20.iloc[-1]['MA']
        
        if current_price > ma5_val and ma5_val > ma20_val:
            return 'long'  # 上涨趋势
        elif current_price < ma5_val and ma5_val < ma20_val:
            return 'short'  # 下跌趋势
        return 'none'  # 无趋势
    
    def check_pattern(self):
        """检查形态信号（顶底分型）"""
        klines = self.api.get_kline_serial(self.symbol, 5 * 60 * 1000, 20)
        closes = klines['close'].values
        
        # 简化：检查最近3根K线
        last3 = closes[-3:]
        
        # 底分型（做多信号）
        if last3[0] > last3[1] and last3[1] < last3[2]:
            return 'long_signal'
        # 顶分型（做空信号）
        elif last3[0] < last3[1] and last3[1] > last3[2]:
            return 'short_signal'
        return 'none'
    
    def calculate_position_size(self, stop_loss_price):
        """计算开仓手数"""
        risk_amount = self.account * self.risk_ratio
        price_diff = abs(self.api.get_quote(self.symbol).last_price - stop_loss_price)
        
        # 获取合约信息
        contract = self.api.get_contract(self.symbol)
        multiplier = contract.volume_tick  # 合约乘数
        margin = contract.margin  # 保证金比例
        
        position_size = risk_amount / (price_diff * multiplier * margin)
        return int(position_size)
    
    def on_bar(self):
        """K线触发执行"""
        # 获取当前持仓
        position = self.api.get_position(self.symbol)
        self.position = position.pos_long - position.pos_short
        
        # 检查是否需要交易
        trend = self.check_trend()
        pattern = self.check_pattern()
        
        if self.position == 0:  # 无持仓
            if trend == 'long' and pattern == 'long_signal':
                # 做多信号
                price = self.api.get_quote(self.symbol).last_price
                stop_loss = price * (1 - self.stop_loss_pct)
                size = self.calculate_position_size(stop_loss)
                self.open_long(size, price)
                
            elif trend == 'short' and pattern == 'short_signal':
                # 做空信号
                price = self.api.get_quote(self.symbol).last_price
                stop_loss = price * (1 + self.stop_loss_pct)
                size = self.calculate_position_size(stop_loss)
                self.open_short(size, price)
        else:
            # 有持仓，检查止盈止损
            self.check_exit()
    
    def open_long(self, size, price):
        """开多仓"""
        print(f"开多仓: {self.symbol}, 数量: {size}, 价格: {price}")
        self.api.insert_order(self.symbol, 'BUY', 'OPEN', size, price)
        self.position = size
        self.entry_price = price
        
    def open_short(self, size, price):
        """开空仓"""
        print(f"开空仓: {self.symbol}, 数量: {size}, 价格: {price}")
        self.api.insert_order(self.symbol, 'SELL', 'OPEN', size, price)
        self.position = -size
        self.entry_price = price
        
    def check_exit(self):
        """检查平仓"""
        current_price = self.api.get_quote(self.symbol).last_price
        
        if self.position > 0:  # 持多仓
            profit_pct = (current_price - self.entry_price) / self.entry_price
            
            # 止损
            if profit_pct < -self.stop_loss_pct:
                self.close_position()
                print(f"止损平多仓: 亏损 {profit_pct*100:.2f}%")
            
            # 止盈分批
            for i, target in enumerate(self.profit_target):
                if profit_pct >= target and not hasattr(self, f'tp{i}_done'):
                    close_size = abs(self.position) // 3
                    self.api.insert_order(self.symbol, 'SELL', 'CLOSE', close_size, current_price)
                    setattr(self, f'tp{i}_done', True)
                    print(f"止盈平多仓: 盈利 {profit_pct*100:.2f}%")
                    
        elif self.position < 0:  # 持空仓
            profit_pct = (self.entry_price - current_price) / self.entry_price
            
            # 止损
            if profit_pct < -self.stop_loss_pct:
                self.close_position()
                print(f"止损平空仓: 亏损 {profit_pct*100:.2f}%")
            
            # 止盈分批
            for i, target in enumerate(self.profit_target):
                if profit_pct >= target and not hasattr(self, f'tp{i}_done'):
                    close_size = abs(self.position) // 3
                    self.api.insert_order(self.symbol, 'BUY', 'CLOSE', close_size, current_price)
                    setattr(self, f'tp{i}_done', True)
                    print(f"止盈平空仓: 盈利 {profit_pct*100:.2f}%")
    
    def close_position(self):
        """全部平仓"""
        if self.position > 0:
            self.api.insert_order(self.symbol, 'SELL', 'CLOSE', abs(self.position), 
                                 self.api.get_quote(self.symbol).last_price)
        elif self.position < 0:
            self.api.insert_order(self.symbol, 'BUY', 'CLOSE', abs(self.position),
                                 self.api.get_quote(self.symbol).last_price)
        self.position = 0
        
    def run(self):
        """主循环"""
        print(f"策略启动: {self.symbol}")
        while True:
            try:
                self.on_bar()
                time.sleep(60)  # 每分钟检查一次
            except Exception as e:
                print(f"错误: {e}")
                time.sleep(10)

# 配置
config = {
    'account': '你的天勤账号',
    'password': '你的密码',
    'symbol': 'SHFE.rb2505',  # 螺纹钢
    'account_equity': 100000,  # 账户权益10万
    'risk_ratio': 0.01,        # 1%风险
}

# 运行
strategy = TrendFollowingStrategy(config)
strategy.run()
```

---

## 七、策略参数一览

| 参数 | 默认值 | 可调范围 | 说明 |
|------|--------|----------|------|
| 风险比例 | 1% | 0.5-2% | 单笔最大亏损 |
| MA周期 | 20 | 10-60 | 趋势判断周期 |
| 止损比例 | 1.5% | 1-3% | 止损幅度 |
| 止盈目标 | 3/5/8% | 自定义 | 分批止盈 |
| 交易时段 | 日盘+夜盘 | 可选 | 交易时间 |

---

## 八、注意事项

1. **模拟盘测试**：先模拟盘跑 3 个月验证
2. **品种选择**：建议先做螺纹钢(RB)，流动性好
3. **时间周期**：新手建议用15分钟，避免5分钟太频繁
4. **复盘总结**：每日收盘后复盘，记录交易日志

---

这个策略框架是否符合您的预期？需要我修改某些部分吗？