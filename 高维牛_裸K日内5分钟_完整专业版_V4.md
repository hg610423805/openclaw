# 高维牛裸K日内5分钟趋势交易策略 - 完整专业版

> 版本：V4.0 最终版
> 周期：5分钟K线
> 品种：螺纹钢(RB)、热卷(HC)、焦炭(J)、甲醇(MA)、玻璃(FG)
> 核心理念：高维牛《通向稳定盈利之路》+ 走势结构 + 裸K形态 + 矢量型
> 作者：咕噜 AI（学习 Q000-Q114 系列文档）
> 日期：2026-03-19

---

# 第一部分：核心理念

## 1.1 稳定盈利的三个层次

| 层次 | 含义 | 做到难度 |
|------|------|----------|
| **结构简单** | 交易系统简单，信号明确 | ⭐ |
| **操作简单** | 看到信号就执行，不犹豫 | ⭐⭐ |
| **心理简单** | 做到不贪不惧，无我交易 | ⭐⭐⭐ |

## 1.2 走势终将完美

```
上涨趋势：高点不断太高，低点不断太高
下跌趋势：高点不断降低，低点不断降低
盘整：高不创新高低不创新低
```

## 1.3 核心原则

> **"做期货就是做概率，做走势结构，做确定性"**

1. 轻仓：单笔风险 ≤ 1-2%
2. 顺势：只做趋势明确的方向
3. 止损：绝不允许扛单（死扛=爆仓）
4. 持仓：让利润奔跑
5. 休息：连续亏损必须停止

---

# 第二部分：裸K核心知识

## 2.1 K线基础

| K线类型 | 含义 | 信号 |
|---------|------|------|
| 大阳线（>5%）| 多方强势 | 上涨中继/启动 |
| 大阴线（>5%）| 空方强势 | 下跌中继/启动 |
| 十字星 | 多空平衡 | 可能转折 |
| 上影线 | 上方有压力 | 滞涨信号 |
| 下影线 | 下方有支撑 | 止跌信号 |

## 2.2 顶底分型（最关键信号）

```
顶分型（做空信号）：
┌─┐
└─┘ └─┐    ← 中间K线最高
┌─┘ └─┘
└─┘
条件：中间K线最高点最高 + 收盘价最低

底分型（做多信号）：
└─┘
┌─┘ └─┐    ← 中间K线最低
└─┘ ┌─┘
└─┘
条件：中间K线最低点最低 + 收盘价最高
```

## 2.3 包含关系处理

```
向上处理（上涨趋势）：
取两K线最高点的更高、收盘价的更高

向下处理（下跌趋势）：
取两K线最低点的更低、收盘价的更低
```

## 2.4 走势类型

| 类型 | 特征 | 交易方向 |
|------|------|----------|
| 上涨 | 高点抬高、低点抬高 | 只做多 |
| 下跌 | 高点降低、低点降低 | 只做空 |
| 盘整 | 高不创新高、低不创新低 | 不做/看戏 |

## 2.5 矢量型（死鱼2核心）

```
矢量向上：
- 每一波上涨的高点 > 上一波高点
- 每一波下跌的低点 > 上一波低点

矢量向下：
- 每一波下跌的低点 < 上一波低点
- 每一波上涨的高点 < 上一波高点
```

## 2.6 矢量衰竭信号

```
上涨衰竭：
- 价格创新高，MACD不创新高（顶背离）
- 上涨速度明显放缓
- 成交量萎缩
- 出现射击之星

下跌衰竭：
- 价格创新低，MACD不创新低（底背离）
- 下跌速度明显放缓
- 成交量萎缩
- 出现锤子/倒锤子
```

---

# 第三部分：入场条件（5分钟级别）

## 3.1 趋势判断（第一步）

```
【上涨趋势】满足≥3个：
□ MA20方向向上
□ 价格在MA20均线上方
□ 低点不断太高（最近3波）
□ 上涨角度>45度

【下跌趋势】满足≥3个：
□ MA20方向向下
□ 价格在MA20均线下方
□ 高点不断降低（最近3波）
□ 下跌角度>45度

【无趋势】MA20走平 → 禁止交易！
```

## 3.2 入场信号（第二步）

### 信号A：分型突破（首选，胜率最高）

```
【做多】
条件：
1. 确认上涨趋势
2. 出现底分型
3. 底分型后价格突破最高点（收盘价突破）
4. 突破时成交量放大（量能确认）

入场点：突破K线收盘价
止损点：底分型最低点下方0.5%

【做空】
条件：
1. 确认下跌趋势
2. 出现顶分型
3. 顶分型后价格跌破最低点（收盘价跌破）
4. 跌破时成交量放大

入场点：跌破K线收盘价
止损点：顶分型最高点上方0.5%
```

### 信号B：回踩均线（稳健型）

```
【做多回踩】
1. 上涨趋势中
2. 价格回踩MA20/MA60
3. 出现看涨K线组合（锤子、吞没、早晨之星）
4. 在均线支撑位企稳

入场点：K线企稳收盘价
止损点：均线下方1%

【做空回抽】
1. 下跌趋势中
2. 价格回抽MA20/MA60
3. 出现看跌K线组合（射击之星、吞没、黄昏星）
4. 在均线压力位受阻

入场点：K线受阻收盘价
止损点：均线上方1%
```

### 信号C：突破前高/低（动量型）

```
【突破高点做多】
1. 价格突破最近3根5分钟K线最高点
2. 突破时成交量 > 近期平均量能1.5倍
3. 突破K线实体 > 50%

入场点：突破K线收盘价
止损点：突破K线最低点下方0.5%

【跌破低点做空】
1. 价格跌破最近3根5分钟K线最低点
2. 跌破时成交量放大
3. 跌破K线实体 > 50%

入场点：跌破K线收盘价
止损点：跌破K线最高点上方0.5%
```

### 信号D：矢量衰竭转折（高赔率）

```
【做多转折】
1. 下跌趋势末端
2. 出现矢量衰竭信号（背离+缩量）
3. 出现标准底分型
4. 放量突破

入场点：底分型突破点
止损点：底分型最低点下方1%

【做空转折】
1. 上涨趋势末端
2. 出现矢量衰竭信号
3. 出现标准顶分型
4. 放量跌破

入场点：顶分型跌破点
止损点：顶分型最高点上方1%
```

## 3.3 过滤条件（提高准确率）

```
【禁止入场】
□ 震荡行情（MA20走平）
□ 夜盘开盘30分钟内
□ 午盘收盘前15分钟
□ 涨停/跌停附近3跳内
□ 持仓量急剧下降
□ 心情不好/状态不佳
□ 连续亏损3笔后

【优先入场】
□ 突破重要压力/支撑位
□ 教科书级标准形态
□ 连续下跌/上涨后的反转
□ 均线多头/空头排列
□ 突破盘整区间
```

---

# 第四部分：仓位管理

## 4.1 账户分级

| 账户规模 | 单笔风险 | 最大持仓 | 单日最大亏损 |
|----------|----------|----------|--------------|
| 1-3万    | 1%       | 1个合约  | 3%           |
| 3-10万   | 1%       | 2个合约  | 3%           |
| 10-50万  | 0.8%     | 3个合约  | 2.5%         |
| 50万以上 | 0.5%     | 5个合约  | 2%           |

## 4.2 手数计算公式

```python
单笔风险金额 = 账户权益 × 风险比例
止损价差 = |入场价 - 止损价|
每手风险 = 止损价差 × 合约乘数 × 保证金比例
开仓手数 = 单笔风险金额 / 每手风险
```

## 4.3 品种选择优先级

1. 成交量 > 100万手/日（流动性好）
2. 波动适中（螺纹钢最佳）
3. 趋势明显时做龙一品种
4. 避免交易不活跃品种

---

# 第五部分：出场规则

## 5.1 止损（铁律）

```
【硬止损】（无条件执行）
- 收盘跌破MA20 → 无条件平仓
- 底分型最低点被跌破 → 平多
- 顶分型最高点被涨破 → 平空
- 单笔亏损 > 2% → 必须平仓

【软止损】
- 浮盈后回撤50% → 移动到保本
- 达到目标位 → 部分止盈
```

## 5.2 止盈（分批离场）

```
【方案A：固定目标】
- +2% 止盈1/3
- +4% 止盈1/3  
- +6% 止盈最后1/3

【方案B：移动止盈】
- 浮盈>3%，跌破MA5 → 止盈1/3
- 浮盈>5%，跌破MA10 → 止盈1/3
- 浮盈>8%，跌破MA20 → 全部平仓

【方案C：形态终结】
- 出现反向顶/底分型
- 趋势破坏（高低点被突破）
- 巨量反转
```

## 5.3 时间规则

```
【日内规则】
- 14:45后只平仓不开新仓
- 夜盘21:00-21:30不新开仓
- 02:30前必须全部平仓

【收盘前检查】
- 浮盈<1%必须平
- 有浮盈可持到底
```

---

# 第六部分：风控体系

## 6.1 每日风控

| 条件 | 动作 |
|------|------|
| 单日亏损 ≥ 2% | 停止当日交易 |
| 单日交易 > 5次 | 停止交易 |
| 连续2天亏损 | 强制休息1天 |

## 6.2 每周风控

| 条件 | 动作 |
|------|------|
| 周亏损 ≥ 5% | 停止本周交易 |
| 周交易 > 20次 | 仓位减半 |

## 6.3 每月风控

| 条件 | 动作 |
|------|------|
| 月亏损 ≥ 10% | 停止本月交易 |
| 月回撤 ≥ 15% | 全面检修系统 |

## 6.4 十大禁止（高压线）

```
❌ 逆势抄底摸顶
❌ 亏损加仓（摊平成本）
❌ 死扛不止损
❌ 重仓（>30%）
❌ 频繁交易（>10次/日）
❌ 情绪化交易
❌ 报复性交易
❌ 听消息交易
❌ 臆想交易
❌ 不复盘总结
```

---

# 第七部分：天勤量化完整代码

```python
# -*- coding: utf-8 -*-
"""
高维牛裸K日内5分钟趋势策略 - 完整专业版 V4
天勤量化 TQSDK
"""

from tqsdk import TqApi, TqAuth
from tqsdk.ta import MA, MACD
import pandas as pd
import numpy as np
from datetime import datetime, time, timedelta
import sys

class GaoWeiNiuNakedKStrategy:
    """高维牛裸K趋势策略"""
    
    def __init__(self, config):
        # 基础配置
        self.api = TqApi(auth=TqAuth(config['account'], config['password']))
        self.symbols = config.get('symbols', ['SHFE.rb2505'])
        self.account_equity = config['account_equity']
        
        # 策略参数
        self.risk_ratio = config.get('risk_ratio', 0.01)        # 1%风险
        self.max_position = config.get('max_position', 30)      # 最大30%
        self.max_daily_loss = config.get('max_daily_loss', 0.02) # 日最大2%
        self.stop_loss_pct = config.get('stop_loss_pct', 0.005)  # 0.5%止损
        
        # 均线参数
        self.ma_period = config.get('ma_period', 20)
        
        # 止盈目标
        self.tp_levels = config.get('tp_levels', [0.02, 0.04, 0.06])
        
        # 状态
        self.positions = {}
        self.daily_pnl = 0
        self.daily_trades = 0
        self.consecutive_loss = 0
        
    # ==================== 核心计算 ====================
    
    def get_klines(self, symbol, period=5*60*1000, length=100):
        return self.api.get_kline_serial(symbol, period, length)
    
    def calc_ma(self, klines, period=20):
        return MA(klines, period)
    
    def check_trend(self, klines):
        """判断趋势方向"""
        ma = self.calc_ma(klines, self.ma_period)
        ma_val = ma.iloc[-1]['MA']
        ma5_val = self.calc_ma(klines, 5).iloc[-1]['MA']
        current = klines.iloc[-1]['close']
        
        # 趋势判断
        if current > ma_val and ma5_val > ma_val and ma_val > ma.iloc[-3]['MA']:
            return 'uptrend'
        elif current < ma_val and ma5_val < ma_val and ma_val < ma.iloc[-3]['MA']:
            return 'downtrend'
        return 'sideway'
    
    def find_fractal(self, klines):
        """识别顶底分型"""
        if len(klines) < 5:
            return 'none'
        
        recent = klines.tail(5).reset_index(drop=True)
        mid = recent.iloc[2]
        before = recent.iloc[1]
        after = recent.iloc[3]
        
        # 底分型
        if (mid['low'] < before['low'] and 
            mid['low'] < after['low'] and
            mid['close'] > before['close'] and 
            mid['close'] > after['close']):
            return 'bottom'
        
        # 顶分型
        if (mid['high'] > before['high'] and 
            mid['high'] > after['high'] and
            mid['close'] < before['close'] and 
            mid['close'] < after['close']):
            return 'top'
        
        return 'none'
    
    def check_breakout(self, klines, direction='up'):
        """检查突破"""
        if len(klines) < 4:
            return False, 0
        
        recent = klines.tail(3)
        
        if direction == 'up':
            high = recent['high'].max()
            return klines.iloc[-1]['close'] > high, high
        else:
            low = recent['low'].min()
            return klines.iloc[-1]['close'] < low, low
    
    def check_volume(self, klines):
        """量能检查"""
        if len(klines) < 20:
            return False
        avg_vol = klines['volume'].iloc[-20:].mean()
        return klines.iloc[-1]['volume'] > avg_vol * 1.5
    
    def check_divergence(self, klines, direction='up'):
        """检查背离（矢量衰竭）"""
        try:
            macd = MACD(klines, 12, 26, 9)
            
            if direction == 'up':
                # 底背离：价格新低，MACD不新低
                prices = klines['low'].tail(5)
                macds = macd.iloc[-5:]['dea']
                
                if prices.min() == prices.iloc[-1] and macds.min() != macds.iloc[-1]:
                    return True
            else:
                # 顶背离：价格新高，MACD不新高
                prices = klines['high'].tail(5)
                macds = macd.iloc[-5:]['dea']
                
                if prices.max() == prices.iloc[-1] and macds.max() != macds.iloc[-1]:
                    return True
        except:
            pass
        return False
    
    def check_vector_momentum(self, klines):
        """检查矢量动量"""
        if len(klines) < 15:
            return 'neutral'
        
        # 简单判断：最近5波方向
        recent = klines.tail(10)
        highs = recent['high'].values
        lows = recent['low'].values
        
        # 上涨波：高低点都抬高
        up_waves = sum(1 for i in range(1, len(highs)-1) if highs[i] > highs[i-1] and highs[i] > highs[i+1])
        down_waves = sum(1 for i in range(1, len(highs)-1) if highs[i] < highs[i-1] and highs[i] < highs[i+1])
        
        if up_waves > down_waves:
            return 'bullish'
        elif down_waves > up_waves:
            return 'bearish'
        return 'neutral'
    
    # ==================== 交易信号 ====================
    
    def get_entry_signal(self, symbol):
        """获取入场信号"""
        klines = self.get_klines(symbol)
        
        # 1. 趋势判断
        trend = self.check_trend(klines)
        
        # 2. 分型识别
        fractal = self.find_fractal(klines)
        
        # 3. 突破检查
        up_break, up_level = self.check_breakout(klines, 'up')
        down_break, down_level = self.check_breakout(klines, 'down')
        
        # 4. 量能确认
        vol_ok = self.check_volume(klines)
        
        # 5. 矢量动量
        momentum = self.check_vector_momentum(klines)
        
        current = klines.iloc[-1]['close']
        
        # ===== 入场条件 =====
        
        # 信号A：分型突破（首选）
        if trend == 'uptrend' and fractal == 'bottom' and up_break and vol_ok:
            stop = klines.tail(5)['low'].min() * 0.995
            return {
                'type': 'A',
                'direction': 'long',
                'entry': current,
                'stop_loss': stop,
                'signal': '上涨趋势+底分型突破+放量'
            }
        
        if trend == 'downtrend' and fractal == 'top' and down_break and vol_ok:
            stop = klines.tail(5)['high'].max() * 1.005
            return {
                'type': 'A',
                'direction': 'short',
                'entry': current,
                'stop_loss': stop,
                'signal': '下跌趋势+顶分型跌破+放量'
            }
        
        # 信号D：矢量衰竭转折（高赔率）
        if trend == 'downtrend':
            divergence = self.check_divergence(klines, 'up')
            if divergence and fractal == 'bottom' and vol_ok:
                stop = klines.tail(5)['low'].min() * 0.99
                return {
                    'type': 'D',
                    'direction': 'long',
                    'entry': current,
                    'stop_loss': stop,
                    'signal': '下跌末端+底背离+底分型+放量'
                }
        
        if trend == 'uptrend':
            divergence = self.check_divergence(klines, 'down')
            if divergence and fractal == 'top' and vol_ok:
                stop = klines.tail(5)['high'].max() * 1.01
                return {
                    'type': 'D',
                    'direction': 'short',
                    'entry': current,
                    'stop_loss': stop,
                    'signal': '上涨末端+顶背离+顶分型+放量'
                }
        
        return None
    
    def get_exit_signal(self, symbol):
        """获取出场信号"""
        if symbol not in self.positions:
            return None
        
        pos = self.positions[symbol]
        klines = self.get_klines(symbol)
        current = klines.iloc[-1]['close']
        
        entry = pos['entry']
        direction = pos['direction']
        
        # 计算盈亏
        if direction == 'long':
            pnl_pct = (current - entry) / entry
        else:
            pnl_pct = (entry - current) / entry
        
        # 1. 止损
        if direction == 'long' and current < pos['stop_loss']:
            return {'action': 'stop_loss', 'reason': '触发止损'}
        if direction == 'short' and current > pos['stop_loss']:
            return {'action': 'stop_loss', 'reason': '触发止损'}
        
        # 2. 止盈（分批）
        for i, tp in enumerate(self.tp_levels):
            tp_key = f'tp{i}_done'
            if pnl_pct >= tp and not pos.get(tp_key, False):
                return {'action': 'partial_tp', 'level': i+1, 'target': tp, 'reason': f'止盈{i+1}'}
        
        # 3. 趋势破坏
        trend = self.check_trend(klines)
        if direction == 'long' and trend != 'uptrend':
            return {'action': 'trend_break', 'reason': '趋势破坏'}
        if direction == 'short' and trend != 'downtrend':
            return {'action': 'trend_break', 'reason': '趋势破坏'}
        
        # 4. 反向分型
        fractal = self.find_fractal(klines)
        if direction == 'long' and fractal == 'top':
            return {'action': 'reverse_signal', 'reason': '出现顶分型'}
        if direction == 'short' and fractal == 'bottom':
            return {'action': 'reverse_signal', 'reason': '出现底分型'}
        
        return None
    
    # ==================== 仓位计算 ====================
    
    def calc_position_size(self, symbol, risk_amount, stop_price):
        """计算开仓手数"""
        quote = self.api.get_quote(symbol)
        entry = quote.last_price
        price_diff = abs(entry - stop_price)
        
        if price_diff == 0:
            price_diff = entry * self.stop_loss_pct
        
        contract = self.api.get_contract(symbol)
        multiplier = contract.volume_tick
        margin = contract.margin
        
        size = risk_amount / (price_diff * multiplier * margin)
        
        max_size = (self.account_equity * self.max_position / 100) / (entry * margin)
        
        return min(int(size), int(max_size))
    
    # ==================== 交易执行 ====================
    
    def open_position(self, symbol, signal):
        """开仓"""
        if not self.can_trade():
            return
        
        entry = signal['entry']
        stop = signal['stop_loss']
        
        risk_amount = self.account_equity * self.risk_ratio
        size = self.calc_position_size(symbol, risk_amount, stop)
        
        if size == 0:
            return
        
        if signal['direction'] == 'long':
            self.api.insert_order(symbol, 'BUY', 'OPEN', size, entry)
        else:
            self.api.insert_order(symbol, 'SELL', 'OPEN', size, entry)
        
        self.positions[symbol] = {
            'direction': signal['direction'],
            'entry': entry,
            'stop_loss': stop,
            'size': size,
            'type': signal['type'],
            'signal': signal['signal'],
            'entry_time': datetime.now()
        }
        
        print(f"开仓: {symbol} {signal['direction']} {size}手 @ {entry} 信号:{signal['signal']}")
        self.daily_trades += 1
    
    def close_position(self, symbol, exit_info):
        """平仓"""
        if symbol not in self.positions:
            return
        
        pos = self.positions[symbol]
        quote = self.api.get_quote(symbol)
        current = quote.last_price
        
        if pos['direction'] == 'long':
            self.api.insert_order(symbol, 'SELL', 'CLOSE', pos['size'], current)
            pnl = (current - pos['entry']) * pos['size']
        else:
            self.api.insert_order(symbol, 'BUY', 'CLOSE', pos['size'], current)
            pnl = (pos['entry'] - current) * pos['size']
        
        if pnl < 0:
            self.consecutive_loss += 1
        else:
            self.consecutive_loss = 0
            
        self.daily_pnl += pnl
        print(f"平仓: {symbol} {exit_info['action']} {exit_info['reason']} 盈亏: {pnl:.2f}")
        
        del self.positions[symbol]
    
    def can_trade(self):
        """检查是否可交易"""
        # 日亏损限制
        if self.daily_pnl < -self.account_equity * self.max_daily_loss:
            print(f"日亏损达到上限，停止交易")
            return False
        
        # 交易次数限制
        if self.daily_trades >= 5:
            print(f"今日交易次数已达上限")
            return False
        
        # 连续亏损限制
        if self.consecutive_loss >= 3:
            print(f"连续亏损3笔，强制休息")
            return False
        
        return True
    
    def run(self):
        """主循环"""
        print("="*60)
        print("高维牛裸K日内5分钟趋势策略 V4 启动")
        print("="*60)
        
        while True:
            try:
                for symbol in self.symbols:
                    # 检查持仓
                    pos = self.api.get_position(symbol)
                    net = pos.pos_long - pos.pos_short
                    
                    if net == 0:
                        # 无持仓，检查入场
                        if self.can_trade():
                            signal = self.get_entry_signal(symbol)
                            if signal:
                                self.open_position(symbol, signal)
                    else:
                        # 有持仓，检查出场
                        if symbol in self.positions:
                            exit_sig = self.get_exit_signal(symbol)
                            if exit_sig:
                                self.close_position(symbol, exit_sig)
                
                import time
                time.sleep(10)
                
            except KeyboardInterrupt:
                print("策略停止")
                break
            except Exception as e:
                print(f"错误: {e}")
                import time
                time.sleep(30)


# ==================== 启动 ====================

if __name__ == "__main__":
    config = {
        'account': '你的天勤账号',
        'password': '你的密码',
        'account_equity': 100000,    # 10万
        'risk_ratio': 0.01,          # 1%
        'max_position': 30,           # 30%
        'max_daily_loss': 0.02,       # 2%
        
        'symbols': [
            'SHFE.rb2505',   # 螺纹钢
            'SHFE.hc2505',  # 热卷
            'DCE.j2505',    # 焦炭
            'CZCE.ma2505',  # 甲醇
        ],
        
        'ma_period': 20,
        'stop_loss_pct': 0.005,
        'tp_levels': [0.02, 0.04, 0.06],
    }
    
    strategy = GaoWeiNiuNakedKStrategy(config)
    strategy.run()
```

---

# 第八部分：交易检查清单

## 交易前检查
- [ ] 趋势方向明确吗？（MA20方向）
- [ ] 入场信号类型？（A/B/C/D哪种）
- [ ] 仓位计算正确？（1%风险）
- [ ] 止损位明确？
- [ ] 符合时间规则？
- [ ] 不是禁止交易情况？

## 交易后检查
- [ ] 按系统执行了吗？
- [ ] 止损坚决吗？
- [ ] 记录交易日志了吗？
- [ ] 今日复盘了吗？

---

# 总结

## 策略核心要点

| 模块 | 核心内容 |
|------|----------|
| **趋势判断** | MA20方向 + 均线位置 + 走势结构 |
| **信号A** | 底/顶分型 + 突破 + 放量（首选） |
| **信号D** | 矢量衰竭 + 背离 + 分型（高赔率） |
| **仓位** | 1%风险，精确计算手数 |
| **止损** | 0.5%，必须坚决 |
| **止盈** | 2%/4%/6% 分批离场 |
| **风控** | 日2%止损，3连亏休息 |

## 高维牛核心语录

> **走势终将完美**
> **结构不变，矢量2**
> **轻仓顺势，止损是信仰**
> **稳定盈利 = 简单系统 + 严格执行 + 良好心态**

---

这个版本够完整专业了吗？请爸爸确认！🐱